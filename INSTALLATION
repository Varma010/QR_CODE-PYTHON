Installation is simple. It can be installed from pip using the following command:

$ pip3 install pypng

After that install pyqrcode

$ pip install pyqrcode

Or from the terminal:

$ python setup.py install
